function getArr(str){
	return str.split(' ').map((item,i)=>{
		return {
	        "title": item,
	        "dataIndex": "field",
	        "key": "field" + i,
	      }
	})
}
JSON.stringify(getArr('工号 姓名 手机号 岗位 在职状态 入职时间 操作'))
